import type {ThemeConfig} from 'antd';

const customTheme: ThemeConfig = {
    token: {
        fontSize: 16,
        fontFamily: "IRANSans",
        colorPrimary: '#04b88b',
    },
};

export default customTheme;